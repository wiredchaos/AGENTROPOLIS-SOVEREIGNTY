import { createServer } from 'node:http';
export function createAdminServer({router,audit,adminToken,host='127.0.0.1'}={}){ if(!adminToken) throw new Error('adminToken required'); const server=createServer((req,res)=>{ const auth=req.headers.authorization||''; if(auth!==`Bearer ${adminToken}`){ audit?.record('admin.denied',{authorization:auth}); res.writeHead(401); return res.end('denied'); } if(req.url==='/admin/circuit-breaker/reset'){ audit?.record('circuit_breaker.reset',{actor:'admin'}); res.end('reset'); } else if(req.url==='/admin/evaluate'){ audit?.record('evaluation.execution',{actor:'admin'}); res.end('evaluated'); } else { res.writeHead(404); res.end('not found'); }}); return {server,host,listen:(port=0)=>server.listen(port,host)}; }
